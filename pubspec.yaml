name: flutter_fbi
description: "Feature. Binder. Interface."
version: 1.3.19
homepage:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  equatable: 2.0.7
  flutter:
    sdk: flutter
  rxdart: 0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
