name: flutter_fbi
description: A state management library for Flutter using Feature-Binder-Interface architecture pattern. Provides clean separation of business logic, state transformation, and UI.
version: 1.5.4
homepage: https://github.com/hyxabrod/flutter_fbi
repository: https://github.com/hyxabrod/flutter_fbi
issue_tracker: https://github.com/hyxabrod/flutter_fbi/issues
documentation: https://github.com/hyxabrod/flutter_fbi#readme

funding:
  - https://github.com/sponsors/hyxabrod

topics:
  - state-management
  - architecture
  - flutter
  - clean-architecture
  - dart3-compatible

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  equatable: ^2.0.7
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
